# FitMate v14.25 – Procedural Normal Body Restore

- Restored the built-in FitMate procedural 3D character as the primary exercise model.
- Removed the dependency on the imported AccuRig model for runtime exercise rendering.
- Changed the character proportions from bodybuilder-style to a more normal athletic build.
- Enabled the procedural model immediately so the old/loading model does not flash first.
- Enabled equipment sizing metrics directly from the built-in rig.
- Kept the existing calibrated exercise motion system and equipment placement logic.
