/**
 * Compatibility entry point for code that imports Supabase from app/lib.
 *
 * The canonical browser client lives in /lib/supabase.ts so the preferred
 * import remains: import { supabase } from "@/lib/supabase".
 */
export { supabase } from "../../lib/supabase";
