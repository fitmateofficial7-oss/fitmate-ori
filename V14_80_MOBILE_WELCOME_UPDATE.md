# FitMate Mobile Welcome Update

- Homepage on mobile now uses a native-app style full-screen welcome screen inspired by the supplied reference.
- Desktop keeps the existing FitMate marketing website.
- Primary `Mulai Sekarang / Start Now` button uses the existing authenticated Get Started flow: active sessions continue to Dashboard, new users go to Register.
- `Sudah punya akun? Masuk / Already have an account? Login` goes directly to `/login`.
- Mobile homepage hides the floating theme/language controls to keep the first-launch screen clean.
- New optimized hero asset: `public/brand/fitmate-mobile-welcome.jpg`.
